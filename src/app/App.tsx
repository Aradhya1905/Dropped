/**
 * App shell — will mount the provider stack (SafeArea, Navigation, Theme,
 * Query) and hand off to RootNavigator.
 *
 * Not yet wired into the root `App.tsx`; that swap happens when navigation
 * and providers are installed. Placeholder so the structure is real.
 */
import React from 'react';

export default function App(): React.JSX.Element | null {
  return null;
}
